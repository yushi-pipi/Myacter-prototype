<header>
  <div class="container">
    <nav class="navbar navbar-expand-lg navbar-extend fixed-top navbar-dark ">
      <a class="navbar-brand" href="/">Myacter</a>
      <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#Navber" aria-controls="Navber"
        aria-expanded="false" aria-label="レスポンシブ・ナビゲーションバー">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse " id="Navber">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
          <li class="nav-item"><%= link_to "Home",   root_path, :class => "nav-link" %></li>
          <% if logged_in? %>
          <li class="nav-item dropdown">
            <a href="#" class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-toggle="dropdown"
              aria-haspopup="true" aria-expanded="false">
              Account<b class="caret"></b>
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <a class="dropdown-item"><%= link_to "log", current_user %></a>
                <a class="dropdown-item"><%= link_to "Settings", edit_user_path(current_user) %></a>
              <div class="dropdown-divider"></div>
              <a class="dropdown-item"><%= link_to "Log out", logout_path, method: :delete %></a>
            </div>
          </li>
          <% else %>
          <li class="nav-item"><%= link_to "Log in", login_path, :class => "nav-link" %></li>
          <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
    </nav>
  </div>
</header>